1.
.env 里的 EPF_DATA_PATH 指向了旧路径 c:/Users/hyy/Desktop/BS_PowerMarket_Simulation_Soft_HYY/code/epf，这个目录在你的机器上不存在。后端的 epf_prob_service.py
  用这个路径去找模型 CSV 文件，自然找不到，返回空数据。

  已经把路径改为 E:/桌面/咸鱼/code/code/epf。重启后端服务后，点击"计算"应该就能正常加载预测数据了。

  ● 重启后端服务后，现在的行为应该是：

  1. 前端显示的 "预测数据范围" 会从 base_price_data 表获取，应该显示正确的 2024 年日期范围
  2. 点击"计算"时，mode=realtime 会先尝试实时推理 → 失败（没有权重文件）→ 回退到 CSV 模式
  3. CSV 模式现在能正确找到 epf/mamba/results/ 下的 CSV 文件（修复了 "manba" 映射问题）